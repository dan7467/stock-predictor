from django.test import TestCase

class NotificationsTesting:
    
    def setUp(self):
        # create some objects
        # e.g: CustomUser.objects.create_user(username='testUser', password='testPw123', email='some@email.com', my_stocks=[])
        self.assertEqual(True)
    
    def test1(self):
        # test the objects created in setUp
        self.assertEqual(True)